apiVersion: v2
name: traefik-umbrella
version: 0.0.1
description: A Traefik umbrella chart with custom middleware
dependencies:
- name: traefik
  version: ">= 3.1.0"  # Replace with desired Traefik version
  repository: https://traefik.github.io/charts
