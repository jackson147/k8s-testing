image:
  repository: registry.gitlab.com/jackson147/wadebridge-gaming-group
  tag: 1.12.10
  devTag: dev
  pullPolicy: IfNotPresent

postgresql:
  enabled: true
  auth:
    username: "wadebridge_user"
    database: "wbgg"
    existingSecret: "wadebridge-postgres-credentials"
    secretKeys:
      userPasswordKey: "password"
